spring:
  application:
    name: MusicPlay
  config:
    import: optional:file:.env[.properties]
  datasource:
    url: jdbc:postgresql://localhost:5432/db
    username: admin
    password: admin123
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
misc:
  dataPath: data.json